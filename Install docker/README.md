# Install Docker and Docker Command

## Install Docker on Linux
```
sudo apt install docker.io
```
## view Docker version info
```
sudo docker version
```
## view Docker system-wide information
```
sudo docker info
```
